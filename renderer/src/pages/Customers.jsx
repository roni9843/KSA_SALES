import React from 'react';
import AddCustomer from '../components/AddCustomer';

const Customers = () => {
    return (
        <div>
            <AddCustomer onAdded={() => {}} />
        </div>
    );
};

export default Customers;
